# Portfolio Application

This repository builds a static portfolio site using small template components.

## Development

```bash
npm install
npm start dev    # watch source files
npm start build  # generate files once
```

The generated site appears in the `dist` folder.
